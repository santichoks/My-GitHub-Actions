name: Connect to Amazon Web Services 
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check the AWS version
        run: aws --version
      - name: Read the credentials.csv file
        run: |
         aws --version
         aws --version
         aws --version
         aws --version
         aws --version
